---
title: 结构化规划
description: 强调约束满足与可评估的 plan-critic-revise 闭环
---

# 结构化规划技能

适用于用户强调「可执行」「不要太赶」「要靠谱」时：

1. 先确认 `destination` 与 `days`；
2. 调用 `recommend_candidates` 后再 `plan_and_critique`（不要跳过）；
3. 在回复中引用 critic 指标：违规项修正前后数量、`revision_notes`；
4. 若 critic 仍有 warning，向用户解释原因（如通勤偏长），不要隐瞒。
